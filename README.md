# NexusAI
A living AI framework designed to think, evolve and grow.

## Development

Install the project in development mode:

```bash
python -m pip install -e .
```

Run the NexusAI core:

```bash
python -m nexusai_core.main
```

Run the test suite:

```bash
python -m unittest discover -s tests
```
